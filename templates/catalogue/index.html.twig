{% extends 'homepage/index.html.twig' %}

{% block title %}Hello CatalogueController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 12px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
{# LOOP PO TRECH
{%  for product in products %}
{%  if (loop. index % 3) == 0 %}
 </div><div class="">
 {% endif%}
 {% endfor %}
 #}
        <div class="row">
<div class="container" style="width: 20%; justify-content: left; background-color: #f1f2f3">Hello.</div>
<div class="container" style="width: 75%; justify-content: right; background-color: lavenderblush">
 <div class="row">
    {%  for product in products %}
            <div class="card" style="width: 18rem;">
                    <img src="..." class="card-img-top" alt="...">
                    <div class="card-body">
                            <a href="{{ path('catalogue_detail', {('id'):  product.id }) }}
"                        class="card-title" style="font-size: 1.5rem">{{ product.name }}</a>
                        <p class="card-text" style="font-size: 12px"> {{ product.description }}</p>


                            <p>Cena: {{ product.price }} Kč.</p>

                        <a href="#" class="btn btn-primary">Objednat</a>
                    </div>
            </div>
        {% endfor %}

 </div>
</div>
        </div>
{% endblock %}
